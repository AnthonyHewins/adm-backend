package controllers

// Shared constants that are general and mostly have to do with HTTP
const (
	// Register errors
	ERR_EMAIL = "email"
	ERR_LATE  = "late"

	// Tools constants
	MAX_DEGREE   = 5
	MAX_ELEMENTS = 100

	// Tools errors
	ERR_CMD              = "cmd"
	ERR_PARAM            = "param"
	ERR_DEGREE           = "deg"
	ERR_LENGTH           = "length"
	ERR_LENGTH_MISMATCH  = "length-mismatch"
	ERR_GENERAL          = "server"
)
